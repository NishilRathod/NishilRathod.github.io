# RESUME — where this build stands

> Read `PLAN.md` first (the spec, stable). This file is the mutable progress log.
> Update it at the end of every session and after each completed step.

**Project:** personal portfolio / calling card for Nishil Rathod
**Location:** `N:\Projects\nishilrathod.github.io`
**Target:** GitHub Pages user site at `https://nishilrathod.github.io`
**Started:** 2026-08-19

---

## Status by step

| # | Step | Status |
|---|------|--------|
| 0 | Project folder, git init, PLAN.md + RESUME.md | done |
| 1 | Vite + React + TS + Tailwind 4 toolchain, fonts, design tokens | done |
| 2 | Typed content modules (`src/content/*.ts`) | done |
| 3 | Components: Section, Hero, Work, Journey, Toolbelt, Connect | done |
| 4 | `useReveal` scroll animation + reduced-motion gate | done |
| 5 | Accessibility + SEO pass | done |
| 6 | Tests + build verification | done — 12/12 passing, build clean |
| 7 | GitHub Actions Pages workflow | done — written, never run (no remote yet) |

Extras done: `public/og.png` social card (generated by `scripts/make_og.py`),
`robots.txt`, `sitemap.xml`, JSON-LD Person schema, SVG favicon.

## Next action

The site builds and runs. Gaps 2, 3 and 4 are now closed. What remains:

1. **Rewrite the hero tagline in his voice** (`src/content/profile.ts`) — still my draft, and it is the first thing anyone reads.
2. Confirm the Toolbelt edits (gap 5 below), WordPress in particular.
3. Get his sign-off on the visual result at `http://localhost:5173` (`npm run dev`).
   Nobody has actually looked at the rendered page yet.
4. Only then: create the GitHub repo and push.

---

## Content gaps — unanswered

These must not be invented. Record answers here as they arrive.

| # | Question | Answer |
|---|----------|--------|
| 1 | Hero tagline / positioning line | **DRAFT written**, marked in `profile.ts`. Needs his rewrite. |
| 2 | Live deployed URLs for the three projects | **ANSWERED** — every "Link" in the résumé PDF points at a GitHub repo, not a deployment. No `liveUrl` is set on any project and no "live" pill renders. Add one only if he deploys something. |
| 3 | Graduated or final year? | **ANSWERED 2026-08-19 — graduated in 2026.** Status line now reads "B.E. Computer Engineering, A.P. Shah Institute of Technology · Class of 2026 · Mumbai, India", and the timeline closes on a "Graduated" entry. |
| 4 | Which email is public? | **ANSWERED 2026-08-19 — `nishilrathod2512@gmail.com`**, the résumé address. Already what `profile.ts` had; no change needed. |
| 5 | Toolbelt edits | I added TypeScript, FastAPI, Docker, Redis, Tailwind (all demonstrably used in his repos) and dropped HTML, CSS, WordPress. Confirm — WordPress in particular was a deliberate cut, not an oversight. |

**Recovered from the PDF's link annotations** (the visible text was just "linkedin"/"github"):
- GitHub — `https://github.com/NishilRathod`
- LinkedIn — `https://www.linkedin.com/in/nishil-rathod-04a8b627a/`
- LeetCode — `https://leetcode.com/u/NishilRathod/`

## Settled decisions (do not re-ask)

- Purpose is a **calling card**, not a recruiter funnel and not a blog.
- **No writing/blog section** — no posts exist, and an empty one reads worse than none.
- **Phone number never appears** on the site. A test asserts this against `dist/`.
- `aisquare-cli` and `pipe` are **occasional contributions**, not owned projects.
  One quiet line under the work cards. Never presented as his own products.
- No skill-percentage bars in the Toolbelt section.
- Colour palette and font choices are lifted from `harshtomar.com`; exact token
  values are in `PLAN.md`.

## Publishing

**LIVE at https://nishilrathod.github.io/** since 2026-08-19.

- Repo: `NishilRathod/NishilRathod.github.io`, public, default branch `main`.
- Pages source is **GitHub Actions**, not the legacy branch build. GitHub
  auto-enabled `build_type: legacy` when the repo was created, which would have
  served the raw repo root instead of `dist/`. It was switched with
  `gh api -X PUT repos/.../pages -f build_type=workflow`. Worth re-checking if
  the live site ever shows source files instead of the app.
- Every push to `main` redeploys. `npm test` gates the deploy, so a failing
  test — including the phone-number check against `dist/` — blocks publishing.

## Notes / surprises

- `aisquare-cli` and `pipe` on his account are **forks** of `AISquare-Studio`
  repos, not repos he owns. Confirmed with him directly — hence the framing above.
- Reference site is Next.js + Turbopack; we are deliberately not matching that
  stack. Vite is lighter and matches what he already runs.

## Verification run on 2026-08-19

- `npm run build` — clean, `tsc -b` passes strict mode.
- `npx vitest run` — 12/12 passing across `App.test.tsx` and `privacy.test.ts`.
- Independent `grep` of `dist/`: zero phone-number fragments, zero contiguous
  `…@gmail.com` strings. The email is assembled at runtime from two halves.
- **Not yet done:** visual review in a real browser. The Chrome extension is not
  connected in this environment, so nothing has actually been *looked at* — only
  asserted against jsdom. Nishil needs to open `http://localhost:5173` and judge
  it, plus check 320px width, keyboard tabbing, and reduced-motion.

## Verifying visually without the Chrome extension

The Claude browser extension is not connected in this environment, but Chrome
itself is, and headless mode works:

```
"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new   --use-gl=angle --use-angle=swiftshader --enable-unsafe-swiftshader   --hide-scrollbars --virtual-time-budget=6000 --window-size=1440,900   --screenshot=out.png http://localhost:5173/
```

`--disable-gpu` must NOT be passed — it kills WebGL and the scene silently
falls back.

**Never use this recipe to judge narrow widths.** Chrome on Windows clamps the
window to a minimum of about 500px, so `--window-size=390,1200` lays the page
out at **500px** and then crops the image to 390. Text runs under the crop and
the result looks exactly like a horizontal overflow that is not there. This
cost a false bug report on 2026-09-03: the plain-text page was declared broken
on mobile when it wraps correctly at 390px and at 320px.

Verify it by screenshotting a page that prints its own width:

```
<div id="o"></div><script>o.textContent = innerWidth</script>
```

At `--window-size=390,200` it reports 500. For real narrow-width checks use a
browser with proper device emulation, which overrides the layout viewport
rather than resizing a window; measure with
`document.documentElement.scrollWidth > clientWidth` instead of trusting a
picture.

Two more gotchas when screenshotting below the fold:

- The hero uses `min-h-[90svh]`, so a tall window just stretches it. Screenshot
  at a normal height and scroll instead.
- Headless virtual time does not deliver IntersectionObserver callbacks, so
  every scroll-reveal stays at opacity 0. A throwaway same-origin page in
  `public/` that iframes `/` and injects a style forcing
  `opacity:1 !important` works. **Delete it afterwards — anything in `public/`
  ships.**

## Known compromises

- `public/og.png` uses Georgia + Segoe UI, not the site's Fraunces + Geist.
  Pillow cannot read woff2 and converting would need fontTools + brotli. Same
  serif/sans pairing; regenerate with `python scripts/make_og.py` after editing.
- The build emits Cyrillic and Vietnamese font subsets that will never be used.
  They are `unicode-range`-gated so browsers never download them — dead weight in
  `dist/`, not on the wire.
